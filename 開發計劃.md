# 🐼 熊貓射擊遊戲 - 開發計劃

## 📋 遊戲概述

| 項目 | 說明 |
|------|------|
| **遊戲名稱** | 熊貓戰機 (Panda Fighter) |
| **遊戲類型** | 直向卷軸射擊遊戲 (Vertical Scrolling Shooter) |
| **目標平台** | Web (HTML5) |
| **遊戲引擎** | Godot 4.x |
| **畫風風格** | Pixel Art 可愛風格 |
| **主角** | 熊貓飛行員 |
| **關卡數** | 5 個主關卡 + 最終 Boss |
| **Boss** | 機械恐龍 |

---

## 🎮 遊戲玩法

### 核心機制
- **移動**：玩家控制熊貓戰機左右上下移動
- **射擊**：自動射擊或手動射擊
- **道具**：收集道具升級武器
- **生命**：3 條生命，被擊中減少
- **分數**：擊落敵機獲得分數

### 特殊系統
- **熊貓氣場**：蓄力釋放特殊攻擊
- **竹子弹藥**：特殊彈藥類型
- **連擊系統**：連續擊落獎勵倍率

---

## 🎨 美術規格

### Pixel Art 風格
```
分辨率：320x480 (遊戲畫面)
縮放：4x (最終輸出 1280x1920)
色調：明亮可愛，高飽和度
```

### 角色設計

#### 主角 - 熊貓戰機
```
尺寸：32x32 pixels
狀態：
  -  idle：懸浮待機
  -  move：移動動畫 (4 幀)
  -  shoot：射擊動畫 (2 幀)
  -  hit：被擊中 (3 幀)
  -  special：特殊攻擊 (6 幀)
```

#### 敵人類型
| 敵人 | 尺寸 | 描述 |
|------|------|------|
| 小鳥戰機 | 24x24 | 基礎敵人，來回移動 |
| 蜜蜂轟炸機 | 28x28 | 投擲炸彈 |
| 烏鴉偵察機 | 20x20 | 快速，追蹤玩家 |
| 貓頭鷹護衛 | 36x36 | 高血量，發射子彈 |
| 老鷹精英 | 40x40 | 發射多方向子彈 |

#### Boss - 機械恐龍
```
尺寸：128x128 pixels
階段：
  - 階段 1：噴火攻擊
  - 階段 2：發射導彈
  - 階段 3：狂暴模式 (全螢幕攻擊)
```

---

## 🗺️ 關卡設計

### 關卡 1：竹林訓練場
```
主題：綠色竹林
敵人：小鳥戰機 x20
中 Boss：蜜蜂轟炸機 x3
背景：滾動竹林，陽光穿透
難度：★☆☆☆☆
```

### 關卡 2：雲層巡航
```
主題：白色雲朵
敵人：烏鴉偵察機 x25, 小鳥 x15
中 Boss：貓頭鷹護衛 x2
背景：多層雲層視差
難度：★★☆☆☆
```

### 關卡 3：山谷突襲
```
主題：峽谷地形
敵人：貓頭鷹 x20, 蜜蜂 x15
中 Boss：老鷹精英 x3
背景：峽谷兩側岩石
難度：★★★☆☆
```

### 關卡 4：工廠入侵
```
主題：機械工廠
敵人：所有類型混合 x40
中 Boss：機械蜘蛛 x2
背景：工廠內部，齒輪轉動
難度：★★★★☆
```

### 關卡 5：恐龍巢穴 (Boss 戰)
```
主題：火山巢穴
敵人：精英守衛 x30
Boss：機械恐龍
背景：熔岩流動，火山噴發
難度：★★★★★
```

---

## 🔧 技術架構

### Godot 項目結構
```
panda-fighter/
├── project.godot              # 項目配置
├── export_presets.cfg         # Web 導出配置
├── scenes/
│   ├── Main.tscn              # 主場景
│   ├── Game.tscn              # 遊戲場景
│   ├── Player.tscn            # 玩家場景
│   ├── Enemy.tscn             # 敵人基礎場景
│   ├── Boss.tscn              # Boss 場景
│   ├── Bullet.tscn            # 子彈場景
│   ├── PowerUp.tscn           # 道具場景
│   └── UI/
│       ├── HUD.tscn           # 遊戲介面
│       ├── Menu.tscn          # 主選單
│       └── GameOver.tscn      # 結束畫面
├── scripts/
│   ├── player.gd              # 玩家控制
│   ├── enemy.gd               # 敵人 AI
│   ├── boss.gd                # Boss AI
│   ├── bullet.gd              # 子彈邏輯
│   ├── powerup.gd             # 道具邏輯
│   ├── game_manager.gd        # 遊戲管理
│   └── level_loader.gd        # 關卡加載
├── assets/
│   ├── sprites/
│   │   ├── player/            # 玩家精靈
│   │   ├── enemies/           # 敵人精靈
│   │   ├── boss/              # Boss 精靈
│   │   ├── bullets/           # 子彈精靈
│   │   └── effects/           # 特效精靈
│   ├── backgrounds/           # 背景圖片
│   ├── audio/
│   │   ├── music/             # 背景音樂
│   │   └── sfx/               # 音效
│   └── fonts/                 # 字體
└── levels/
    ├── level1.tres            # 關卡 1 配置
    ├── level2.tres            # 關卡 2 配置
    ├── level3.tres            # 關卡 3 配置
    ├── level4.tres            # 關卡 4 配置
    └── level5.tres            # 關卡 5 配置
```

### 核心腳本範例

#### player.gd
```gdscript
extends CharacterBody2D

@export var speed: float = 300.0
@export var health: int = 3
@export var bullet_scene: PackedScene

var can_shoot: bool = true
var shoot_delay: float = 0.15
var power_level: int = 1

func _physics_process(delta):
    var velocity = Vector2.ZERO
    
    if Input.is_action_pressed("move_right"):
        velocity.x += 1
    if Input.is_action_pressed("move_left"):
        velocity.x -= 1
    if Input.is_action_pressed("move_down"):
        velocity.y += 1
    if Input.is_action_pressed("move_up"):
        velocity.y -= 1
    
    self.velocity = velocity.normalized() * speed
    move_and_slide()
    
    # 邊界限制
    position.x = clamp(position.x, 0, get_viewport_rect().size.x)
    position.y = clamp(position.y, 0, get_viewport_rect().size.y)

func shoot():
    if not can_shoot:
        return
    
    var bullet = bullet_scene.instantiate()
    bullet.position = position
    bullet.power = power_level
    get_tree().current_scene.add_child(bullet)
    
    can_shoot = false
    await get_tree().create_timer(shoot_delay).timeout
    can_shoot = true

func take_damage(amount: int):
    health -= amount
    if health <= 0:
        die()

func die():
    # 死亡動畫
    queue_free()
```

#### enemy.gd
```gdscript
extends CharacterBody2D

@export var health: int = 1
@export var speed: float = 100.0
@export var score_value: int = 100
@export var bullet_scene: PackedScene

var direction: float = 1.0

func _physics_process(delta):
    # 向下移動
    position.y += speed * delta
    
    # 左右擺動
    position.x += sin(position.y * 0.05) * 50 * delta
    
    # 射擊
    if randf() < 0.02:  # 2% 機率射擊
        shoot()
    
    # 超出螢幕移除
    if position.y > get_viewport_rect().size.y + 50:
        queue_free()

func shoot():
    var bullet = bullet_scene.instantiate()
    bullet.position = position
    bullet.velocity = Vector2.DOWN * 200
    get_tree().current_scene.add_child(bullet)

func take_damage(amount: int):
    health -= amount
    if health <= 0:
        GameManager.add_score(score_value)
        queue_free()
```

#### game_manager.gd
```gdscript
extends Node

signal score_changed(new_score: int)
signal lives_changed(new_lives: int)
signal game_over
signal level_complete(level: int)

var score: int = 0
var lives: int = 3
var current_level: int = 1
var is_paused: bool = false

func _ready():
    add_to_group("game_manager")

func add_score(points: int):
    score += points
    score_changed.emit(score)

def lose_life():
    lives -= 1
    lives_changed.emit(lives)
    if lives <= 0:
        game_over.emit()

func complete_level():
    level_complete.emit(current_level)
    current_level += 1
    if current_level > 5:
        # 遊戲通關
        pass

func start_game():
    score = 0
    lives = 3
    current_level = 1
    is_paused = false
```

---

## 📅 開發時程

### 第一階段：基礎框架 (1-2 週)
- [ ] Godot 項目設置
- [ ] Web 導出配置
- [ ] 玩家控制系統
- [ ] 子彈系統
- [ ] 基礎 UI (HUD)

### 第二階段：敵人系統 (1-2 週)
- [ ] 敵人基礎 AI
- [ ] 5 種敵人類型
- [ ] 生成系統 (Spawn Manager)
- [ ] 碰撞檢測優化

### 第三階段：關卡設計 (2 週)
- [ ] 5 個關卡配置
- [ ] 背景滾動系統
- [ ] 視差背景效果
- [ ] 中 Boss 戰

### 第四階段：Boss 戰 (1 週)
- [ ] 機械恐龍 Boss
- [ ] 3 階段攻擊模式
- [ ] Boss 血條 UI
- [ ] Boss 戰音樂

### 第五階段：美術音效 (2 週)
- [ ] 熊貓主角精靈 (5 狀態)
- [ ] 敵人精靈 (5 種)
- [ ] Boss 精靈 (3 階段)
- [ ] 背景圖片 (5 關卡)
- [ ] 子彈特效
- [ ] 背景音樂 (5 首)
- [ ] 音效 (射擊/爆炸/道具等)

### 第六階段：優化測試 (1 週)
- [ ] Web 性能優化
- [ ] 移動端適配
- [ ] Bug 修復
- [ ] 平衡性調整
- [ ] 最終測試

**總計：8-10 週**

---

## 🎯 Web 導出配置

### export_presets.cfg
```ini
[preset.0]
name="Web"
platform="Web"
runnable=true
dedicated_server=false
custom_features=""
export_filter="all_resources"
include_filter=""
exclude_filter=""
export_path="export/index.html"
encryption_include_filters=""
encryption_exclude_filters=""
encrypt_pck=false
encrypt_directory=false

[preset.0.options]
custom_template/debug=""
custom_template/release=""
variant/extensions_support=false
variant/thread_support=false
vram_texture_compression/for_desktop=true
vram_texture_compression/for_mobile=false
html/export_icon=true
html/custom_html_shell=""
html/head_include=""
html/canvas_resize_policy=2
html/focus_canvas_on_start=true
html/experimental_virtual_keyboard=false
progressive_web_app/enabled=true
progressive_web_app/ensure_cross_origin_isolation_headers=true
progressive_web_app/offline_page=""
progressive_web_app/display=1
progressive_web_app/orientation=1
progressive_web_app/icon_144x144=""
progressive_web_app/icon_180x180=""
progressive_web_app/icon_512x512=""
progressive_web_app/background_color=Color(0, 0, 0, 1)
```

### 性能優化
```gdscript
# 對象池管理子彈
class BulletPool:
    var pool: Array = []
    var scene: PackedScene
    
    func _init(scene: PackedScene, size: int = 50):
        self.scene = scene
        for i in range(size):
            var bullet = scene.instantiate()
            bullet.set_process(false)
            pool.append(bullet)
    
    func get_bullet() -> Node:
        for bullet in pool:
            if not bullet.is_inside_tree():
                bullet.set_process(true)
                return bullet
        # 池滿時創建新的
        var bullet = scene.instantiate()
        pool.append(bullet)
        return bullet
    
    func return_bullet(bullet: Node):
        bullet.set_process(false)
        bullet.visible = false
```

---

## 🎵 音效清單

### 背景音樂
| 曲目 | 場景 | 長度 |
|------|------|------|
| BGM_01 | 主選單 | 2:00 |
| BGM_02 | 關卡 1-2 | 2:30 |
| BGM_03 | 關卡 3-4 | 2:30 |
| BGM_04 | 關卡 5 | 3:00 |
| BGM_05 | Boss 戰 | 3:30 |
| BGM_06 | 遊戲通關 | 2:00 |
| BGM_07 | 遊戲結束 | 1:30 |

### 音效
| 音效 | 說明 |
|------|------|
| SFX_Shoot | 玩家射擊 |
| SFX_Explosion | 敵人爆炸 |
| SFX_PlayerHit | 玩家被擊中 |
| SFX_PowerUp | 收集道具 |
| SFX_BossRoar | Boss 咆哮 |
| SFX_BossHit | Boss 被擊中 |
| SFX_LevelComplete | 關卡完成 |
| SFX_GameOver | 遊戲結束 |

---

## 📦 交付物

### 最終產品
- [ ] Web 版本 (HTML5)
- [ ] 源代碼 (Godot 項目)
- [ ] 美術資源 (PNG 精靈表)
- [ ] 音頻資源 (OGG/WAV)
- [ ] 遊戲說明文檔

### 技術文檔
- [ ] 代碼註釋
- [ ] API 文檔
- [ ] 部署指南
- [ ] 維護手冊

---

## 💡 擴展建議

### 後續更新 (DLC)
1. **無限模式**：生存挑戰
2. **成就系統**：Steam/Google Play 成就
3. **排行榜**：全球分數排名
4. **新角色**：解鎖其他動物戰機
5. **新關卡**：額外關卡包

### 多平台發布
- [ ] Web (HTML5)
- [ ] Android
- [ ] iOS
- [ ] Windows/Mac/Linux
- [ ] Steam

---

## 📝 備註

- 使用 Godot 4.x 最新版本
- 目標幀率：60 FPS
- Web 包體大小控制：< 50MB
- 支持鍵盤和觸控操作
- 響應式設計適配不同螢幕

---

**開發開始日期**：待定
**預計完成日期**：開發開始後 8-10 週
**項目負責人**：待定

🏛️ 大秦丞相 李斯 監製 | 秦王 陛下 御准
